import "../assets/style/global.scss";

const Did0esMe = ({
  Component,
  pageProps
}: {
  Component: any;
  pageProps: any;
}) => {
  return <Component {...pageProps} />;
};

export default Did0esMe;
